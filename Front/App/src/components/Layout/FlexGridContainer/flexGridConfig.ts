// flexGridConfig.ts

/**
 * Define o número máximo de "colunas" que o FlexGridContainer pode usar
 * como base para calcular larguras (colSpan). 
 * O padrão 12 é comum em sistemas de design.
 */
export const MAX_COLUMNS_BASE = 12;