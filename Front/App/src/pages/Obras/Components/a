 {/* --- SEÇÃO PRODUÇÃO, QUALIDADE, DOCUMENTAÇÃO E COMPONENTES ---
                {/* CLASSE: fieldset-container */}
                <div className="fieldset-container">
                    {/* CLASSE: fieldset */}
                    <fieldset className="fieldset">
                        {/* CLASSE: legend */}
                        <legend className="legend">Dados de Produção e Equipamentos</legend>
                        <h4 style={{ marginBottom: '10px' }}>Produção e Testes</h4>
                        {/* CLASSE: grid-2-cols */}
                        <div className="grid-2-cols">
                            {/* CLASSE: label e input */}
                            <label className="label">Vazão (L/h):
                                <input type="number" step="0.1" name="vazao" value={formData.vazao} onChange={handleChange} className="input" />
                            </label>
                            <label className="label">Tempo Teste (h):
                                <input type="number" step="1" name="tempoTeste" value={formData.tempoTeste} onChange={handleChange} className="input" />
                            </label>
                            <label className="label">Nível Estático (m):
                                <input type="number" step="0.1" name="nivelEstatico" value={formData.nivelEstatico} onChange={handleChange} className="input" />
                            </label>
                            <label className="label">Nível Dinâmico (m):
                                <input type="number" step="0.1" name="nivelDinamico" value={formData.nivelDinamico} onChange={handleChange} className="input" />
                            </label>
                        </div>

                        <h4 style={{ marginTop: '15px', marginBottom: '10px' }}>Componentes Instalados</h4>
                        {/* CLASSE: grid-full */}
                        <div className="grid-full">
                            {/* CLASSE: label e input */}
                            <label className="label">Modelo/Potência da Bomba:
                                <input type="text" name="modeloPotenciaBomba" value={formData.modeloPotenciaBomba} onChange={handleChange} className="input" />
                            </label>
                        </div>
                        {/* CLASSE: grid-full */}
                        <div className="grid-full">
                            {/* CLASSE: label e input */}
                            <label className="label">Profundidade Instalação Bomba (m):
                                <input type="number" step="0.1" name="profundidadeInstalacaoBomba" value={formData.profundidadeInstalacaoBomba} onChange={handleChange} className="input" />
                            </label>
                        </div>
                    </fieldset>

                    {/* CLASSE: fieldset */}
                    <fieldset className="fieldset">
                        {/* CLASSE: legend */}
                        <legend className="legend">Qualidade da Água e Documentação</legend>
                        <h4 style={{ marginBottom: '10px' }}>Qualidade da Água</h4>
                        {/* CLASSE: grid-2-cols */}
                        <div className="grid-2-cols">
                            {/* CLASSE: label e input */}
                            <label className="label">pH da Água:
                                <input type="number" step="0.1" name="phAgua" value={formData.phAgua} onChange={handleChange} className="input" />
                            </label>
                            <label className="label">Turbidez (NTU):
                                <input type="number" step="0.1" name="turbidez" value={formData.turbidez} onChange={handleChange} className="input" />
                            </label>
                        </div>
                        {/* CLASSE: grid-full */}
                        <div className="grid-full">
                            {/* CLASSE: label e input */}
                            <label className="label">Data da Análise Laboratorial:
                                <input type="date" name="dataAnaliseLaboratorial" value={formData.dataAnaliseLaboratorial} onChange={handleChange} className="input" />
                            </label>
                            <label className="label">Laboratório Responsável:
                                <input type="text" name="laboratorioResponsavel" value={formData.laboratorioResponsavel} onChange={handleChange} className="input" />
                            </label>
                        </div>

                        <h4 style={{ marginTop: '15px', marginBottom: '10px' }}>Documentação (Links/Referência)</h4>
                        {/* CLASSE: grid-full */}
                        <div className="grid-full">
                            {/* CLASSE: label e input */}
                            <label className="label">Link Fotos Poço Concluído:
                                <input type="text" name="linkFotosConcluido" value={formData.linkFotosConcluido} onChange={handleChange} className="input" />
                            </label>
                            <label className="label">Link Laudo Análise:
                                <input type="text" name="linkLaudoAnalise" value={formData.linkLaudoAnalise} onChange={handleChange} className="input" />
                            </label>
                            <label className="label">Link Vídeo Inspeção Interna:
                                <input type="text" name="linkVideoInspecao" value={formData.linkVideoInspecao} onChange={handleChange} className="input" />
                            </label>
                        </div>
                    </fieldset>
                </div>


                {/* --- SEÇÃO EQUIPE, OUTORGA E OBSERVAÇÕES --- */}

                 {/* --- SEÇÃO EQUIPE, OUTORGA E OBSERVAÇÕES --- */}
                {/* CLASSE: fieldset (com borda padrão) */}
                <fieldset className="fieldset">
                    {/* CLASSE: legend */}
                    <legend className="legend">Equipe, Outorga e Observações</legend>
                    {/* CLASSE: grid-3-cols */}
                    <div className="grid-3-cols">
                        {/* CLASSE: label e input/select */}
                        <label className="label">Equipe de Perfuração:
                            <input type="text" name="equipePerfuracao" value={formData.equipePerfuracao} onChange={handleChange} required className="input" />
                        </label>
                        <label className="label">Responsável / Supervisor:
                            <input type="text" name="responsavelServicoSupervisor" value={formData.responsavelServicoSupervisor} onChange={handleChange} required className="input" />
                        </label>

                        <label className="label">Status da Outorga/Licença:
                            <select name="statusOutorgaLicenca" value={formData.statusOutorgaLicenca} onChange={handleChange} className="select">
                                {outorgaOptions.map(status => (<option key={status} value={status}>{status}</option>))}
                            </select>
                        </label>
                    </div>
                    {/* CLASSE: grid-full */}
                    <div className="grid-full">
                        {/* CLASSE: label e input */}
                        <label className="label">Número Outorga/Processo:
                            <input type="text" name="numeroOutorgaProcesso" value={formData.numeroOutorgaProcesso} onChange={handleChange} className="input" />
                        </label>
                    </div>

                    {/* CLASSE: label e textarea */}
                    <label className="label" style={{ marginTop: '10px' }}>Observações Gerais:
                        <textarea name="observacoesGerais" value={formData.observacoesGerais} onChange={handleChange} rows={3} className="textarea" />
                    </label>
                </fieldset>